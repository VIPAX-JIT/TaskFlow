# TaskFlow frontend – environment template
# Copy to .env.local for dev, set in the host's env for production.

# Base URL for the backend API. Include /api.
# Dev default (if unset): http://localhost:5000/api
VITE_API_URL=http://localhost:5000/api
